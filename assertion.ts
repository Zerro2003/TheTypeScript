// let id: number | string = "123";
// let numericId = (id as string).toUpperCase(); // assert it's a string

let secretData: any = "Hello from Kigali";
let getLength = (secretData as string).length;
console.log(getLength);
